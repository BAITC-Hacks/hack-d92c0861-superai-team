# Собственные проверочные профили
Это НЕ скрытые профили жюри, а три придуманных regression-сценария в формате официального набора.
Импортировать вместе с CSV ПОСЛЕ реализации backend/import; исходные каталоги остаются прежними.
TEST_CQ_001: низкий Public Speaking и три no_show клуба, но есть critical System Design gap.
Проверка: приоритет релевантного технического шага, объяснение цели, чисел и истории.
TEST_CQ_002: System Design=0; EV_006/EV_007 с prerequisite=2 нельзя предлагать немедленным шагом.
Проверка: доступный базовый шаг вместо обхода prerequisites.
TEST_CQ_003: все навыки цели на уровне 5. Пустой steps с причиной, не выдуманное мероприятие/повышение.
Тесты не доказывают прохождение настоящих скрытых профилей и не должны кодироваться через проверки ID.
