# Проверка объединённой версии

Проверено 23 сентября 2026 на Windows, Python 3.13, Node 24.15.

| Проверка | Результат |
| --- | --- |
| pytest | 79 passed |
| Frontend tests | 41 passed |
| Production build | успешно |
| start.py --prepare-only | установка, проверка данных и сборка успешно |
| Полный набор данных | 200 сотрудников, 40 активностей, 60 навыков, 2743 записи |
| Сквозной тест UI session → настоящий API | успешен |
| Завершение и retry | 71.93 → 73.68%, повтор без второго прироста |
| Импорт, ошибки, доступы, сохранение после рестарта | успешно |
| LLM HTTP-контракт | тестовый HTTP-провайдер, проверка структуры и допустимых ID |
| Настоящий внешний LLM | не подтверждён: нужен действующий проектный ключ |

В браузере проверены вход HR и сотрудника, профиль, HR-сводка, переключение RU/KK/EN и фактическая загрузка двух файлов через кнопки: добавлены три сотрудника и три записи истории. Узкий экран проверен выборочно: горизонтального переполнения страницы не обнаружено. Для браузерного прогона использована отдельная локальная база. Исходный датасет не изменён.

Сквозной тест запускает реальный сервер, загружает построенную страницу и её JS, использует настоящий клиентский api.js/session.js, выполняет активность, импортирует новые ID, проверяет запрет чужих профилей для сотрудника и перезапускает сервер. Внешний AI в нём отключён явно.

Автоматические проверки не заменяют нагрузочные испытания или экспертную оценку качества LLM. Полный мобильный аудит не проводился. Подробный повторяемый сценарий — в корневом README.
