# Optional OpenAI configuration. Keep OPENAI_API_KEY empty in committed files.
OPENAI_API_KEY=

# Optional model override. The AI package uses its centralized default when empty.
OPENAI_MODEL=

# Optional request timeout in seconds. Must be a positive number.
OPENAI_TIMEOUT_SECONDS=8
