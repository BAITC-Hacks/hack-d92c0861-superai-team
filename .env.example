# Server-only. Never copy secrets into VITE_* or commit .env.
DATA_DIR=docs
STATE_PATH=.local/career_quest.sqlite3
HR_TOKEN=
EMPLOYEE_TOKEN=
EMPLOYEE_ID=E0001
# Disabled until the team chooses an approved provider and checks data restrictions.
AI_ENABLED=false
# Full base URL including /v1 when required; adapter appends /chat/completions.
LLM_BASE_URL=
LLM_MODEL=
LLM_API_KEY=
